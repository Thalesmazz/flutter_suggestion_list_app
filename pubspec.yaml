name: login_screen_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0' # Versão atualizada para maior compatibilidade

dependencies:
  flutter:
    sdk: flutter

  # UI & Utilities
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  intl: ^0.19.0 # <-- A DEPENDÊNCIA QUE FALTAVA

  # Navigation
  go_router: ^14.2.7

  # Networking
  http: ^1.2.1

  # Firebase
  firebase_core: ^2.31.0
  firebase_auth: ^4.19.0
  firebase_database: ^10.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

